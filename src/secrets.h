#ifndef _SECRETS
#define _SECRETS
static char *host = (char*)"esp32";
static char *ssid = (char*)"yourwifi";			 // To avoid having to enter it manually
static char *password = (char*)"yourwifipassword"; // To avoid having to enter it manually

#endif
